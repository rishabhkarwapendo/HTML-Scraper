# HTML-Scraper
Python version: 3.8.9 64-bit. (Used for running the python files in the main branch) <br />
If you do not have a developer environment, dependencies, or Python installed, you can run all the files by downloading the exe files in the exe_runnables folder. <br />
